name: tape_measure
description: A Slider replacement widget.
version: 1.0.2
homepage: https://github.com/deakjahn/tape_measure

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  pedantic: ^1.11.0

flutter:
